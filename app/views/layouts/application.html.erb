<!DOCTYPE html>
<html>
  <head>
    <title>LairBnb</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:ital,wght@0,400;0,700;0,800;1,400;1,700;1,800&display=swap" rel="stylesheet">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %> 


    <script src='https://api.mapbox.com/mapbox-gl-js/v2.12.0/mapbox-gl.js'></script>
    <link href='https://api.mapbox.com/mapbox-gl-js/v2.12.0/mapbox-gl.css' rel='stylesheet' />


    <%= javascript_importmap_tags %>
    
    <script src="https://kit.fontawesome.com/5f669c3761.js" crossorigin="anonymous"></script>
  </head>

  <body class="bg-slate-100 text-slate-700">

    <header class="w-full border-b border-b-slate-300 bg-white mb-6">
        <div class="container mx-auto flex justify-between py-4 items-center">
          <h1 class="text-purple-700 text-xl font-bold">
            <%=link_to root_path(sd: params[:sd], ed: params[:ed]) do %>
              <i class="fa-solid fa-igloo mr-3"></i>
              lairbnb
            <% end %>
          </h1>

          <%= render 'search/form' %>
      
          <ul class="text-sm font-bold flex gap-6">
            <% if user_signed_in? %>
              <li><%= link_to "Sign out", destroy_user_session_path, data: { turbo_method: :delete } %></li>
            <% else %>
              <li><%= link_to 'Register', new_user_registration_path, class: "hover:underline hover:text-purple-500" %></li>
              <li><%= link_to 'Sign in', new_user_session_path, class: "bg-purple-500 rounded-full px-3 py-2 text-white hover:bg-purple-600" %></li>
            <% end %>
          </ul>
      </div>
    </header>
    <% if notice %>
    <div>
      <%= notice %>
    <div>
    <% end %>
    <section class="container mx-auto">
      <%= yield %>
    </div>
  </body>
</html>
