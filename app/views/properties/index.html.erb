<h1>Properties</h1>

<div id="properties">
  <% @properties.each do |property| %>
    <% if property.available?(params[:s], params[:e]) %>
      <%= image_tag property.image %>
      £<%= property.base_price %>
      <%= property.name %>
      <p>
        <%= link_to "Show this property", property %>
      </p>
      <% end %>
  <% end %>
</div>

<%= link_to "New property", new_property_path %>
